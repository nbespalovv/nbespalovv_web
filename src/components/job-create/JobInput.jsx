const JobInput = (props) => (
    <div className={"job-input"}>
        <input className={"job-input-title"} type={"text"} placeholder={"Text description..."}/>
    </div>

)

export default JobInput;